import { Router } from 'express';

const router = Router();

// Define route modules here later
// router.use('/auth', authRoutes);
// router.use('/users', userRoutes);
// router.use('/complaints', complaintRoutes);

export default router;
